# 大傻豪学习翻译词典 · 功能路线图

参考对象：句乐部（julebu.co）。**只借鉴交互设计与机制，不使用其句库内容**——
他们的课程包是有版权的成品内容。我们的句子来自用户自己划词入库的真实语境，
这也是我们相对他们的核心优势：练的是你自己读到过的句子，不是编辑部挑的。

---

## 阶段一 · 直接能抄，成本极低 ✅ 已完成

全部是纯前端状态 + 本地存储，不调 AI、不联网、不花 token。

- [x] **连击 + 即时反馈 + Perfect 评分** —— 一次做对累计连击，出错归零；
      单句一次通过标 Perfect
- [x] **`Alt+M` 掌握 / `Alt+N` 生词** —— 逐句标记，掌握的降低出现频率，
      生词的优先出现
- [x] **计时 + 进度 + 下一句预览** —— 本局用时、第几句、下一句提前露一行
- [x] **键盘优先** —— `Enter` 提交 / `Ctrl+'` 听整句 / `Ctrl+;` 显示答案 /
      `Tab` 切换空格（造句练习首版已实现）

## 定位修正（重要）

首版把「造句练习」做偏了：留着 the / of / in 这些功能词当骨架，考的其实是
「这个位置该填哪个词」——那是**词汇题**，不是**表达题**。已按下面的划分重做：

| 页面                       | 挖空策略                  | 考什么               | 题源                    |
| -------------------------- | ------------------------- | -------------------- | ----------------------- |
| 闪卡复习 →「语境填空」题型 | `content-words`（留骨架） | 这个词在句中怎么用   | 生词本例句              |
| 造句练习                   | `all`（整句全挖，零骨架） | 这个意思怎么用英文说 | 内置语料库 / 生词本短句 |

内置语料库：`src/utils/sentence-practice/corpus.ts`，60 句，4~10 词（中位 7），
11 个场景。选句标准是「日常高频 + 不幼稚 + 看得懂但说不出」，**全部原创编写**，
不取自任何课程包或教材，可自由随扩展分发。

## 阶段二 · 值得做，中等成本

- [ ] **按意群分块 + 语法角色标注**（主语/谓语/宾语/状语色块）
      入库时用 AI 一次性切好存起来，之后每次练习零开销、零延迟、零 token。
      **这是解决长句问题的关键**——例句中位数 13 词、最长 28 词，按词挖空是酷刑，
      按意群只有 4~5 块
- [ ] **语法点标签**（如「动名词短语作主语」「bring sb sth 结构」）
      与上一条**共用同一次 AI 调用**，边际成本几乎为零，两条应捆在一起做
- [ ] **AI 英语老师** —— 练习中随时提问「这里为什么用 in 不用 on」。
      复用现成的自定义动作流式接口。延迟可接受（用户主动触发，不像字幕那样卡着看视频）
- [ ] **粘贴文章 → 自动变练习** —— 分句用 `Intl.Segmenter`（已在用），
      翻译走**免费的 Microsoft/Google 引擎**，完全不烧 token

## 阶段三 · 能做但有真实限制

- [ ] **口语评估** —— Chrome 自带 Web Speech API，免费无需 key。
      **能做到**「你念的句子对不对」；**做不到**他们那种发音标准度打分
      （音素级评分需要 Azure 发音评估这类付费服务）。预期要提前说清楚
- [ ] **意群下标音标** —— 两条路：①内置 CMUdict（公有领域，13 万词，体积 +3~4MB）；
      ②AI 预处理时一起生成并缓存。后者更省事，可与阶段二合并成一次调用
- [ ] **分级课程包** —— 技术不难，难在内容。语料来源建议 **Tatoeba**
      （CC-BY 2.0 FR，可商用可分发，需署名），按句长 + 词频筛出 B1~B2 难度带

## 明确不做

- **教师端 / 学生数据看板** —— 需要服务器和账号体系，与「完全本地、不联网、
  免费分发」的架构根本冲突
- **会员体系 / 付费墙** —— 定位就是完全免费，这反而是我们的优势

---

## 阶段一实现记录

| 功能                         | 落点                                                        |
| ---------------------------- | ----------------------------------------------------------- |
| 连击 / Perfect / 计时 / 预览 | `src/entrypoints/options/pages/sentence-practice/index.tsx` |
| 掌握 / 生词 标记与持久化     | `src/utils/sentence-practice/progress.ts`                   |
| 挖空与判定（首版）           | `src/utils/sentence-practice/blanks.ts`                     |

标记存在 `chrome.storage.local`，键 `sentencePracticeProgress`，
跟 FSRS 卡片系统**刻意解耦**——闪卡排的是单词，这里排的是句子，
混在一起会出现「句子写错了把单词的复习间隔也拉近」的怪现象。

---

## 类型检查 · 已清零（pre-push hook 现已全量启用）

`pnpm lint`（= `oxlint --type-aware --type-check`）**0 错误**，
`pnpm fmt:check` **通过**。从此不再使用 `--no-verify`。

### 曾经的根因

`@orpc/contract` 里 `ErrorMap` 的每个键都是可选的：

```ts
type ErrorMap = { [key in ORPCErrorCode]?: ErrorMapItem<AnySchema> }
```

上游契约包的 `pickPublicErrorMap()` 返回 `Pick<ErrorMap, TCodes[number]>`，
`Pick` **保留可选性** → 值变成 `ErrorMapItem | undefined` → ORPC 的构造器映射

```ts
[K in keyof T]: T[K] extends ErrorMapItem<infer U> ? (...) => ORPCError : never
```

匹配失败 → 全塌成 `never` → `errors.XXX()` 报 "not callable"。
**这是上游 `@read-frog/api-contract` 的类型缺陷，不是本项目写错**，12 个错误同一根因。

解法：`src/utils/local-notebase/orpc-errors.ts` 里的 `errs()` 还原调用签名。
键名仍来自契约（错误码拼错照样编译不过），零运行时开销。
等上游把返回类型改成必填后，该文件可整体删除。

### 顺带修掉的类型谎言（都是真问题，不是噪音）

| 位置                                     | 原本声明                  | 实际存的                  |
| ---------------------------------------- | ------------------------- | ------------------------- |
| `storage.ts` `srsLearningSteps`          | `number[]`                | `["1m","10m"]` 步长字符串 |
| `storage.ts` `config`                    | `unknown`                 | 列类型判别联合            |
| `storage.ts` `notebaseViews`             | `unknown[]`               | 契约的 `NotebaseView[]`   |
| `srs-storage.ts` `fsrsReviewLogSnapshot` | `Record<string, unknown>` | 有确定字段的快照          |

这些谎言被四处强转（`as number[]`、`as unknown as string[]`）遮住，
改成真实类型后强转全部删除，`errors: any` 也一并换成结构类型。

### 发布 v1.0.0 时修掉的运行时 bug

- `notebaseColumn.update` / `delete` 的兜底分支调 `errors.NOTEBASE_NOT_FOUND()`，
  但契约里没声明它 —— 运行时是 `undefined`，抛的是 `TypeError` 而不是干净的 404
- `user.ensureTimezone` 少返回契约要求的 `updated` 字段
- `intercept.ts` 的 headers 造 `Record`，契约要 `[string,string][]`，
  靠 `HeadersInit` 恰好两种都吃才没出事

### 测试套件 · 已全绿

277 个测试文件 / 2674 个用例全部通过（此前 51 个文件、193 个用例失败）。
失败几乎全是分叉后的历史漂移，按性质分四类处理：

| 类别                                                                                | 做法                                                                                                                     |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 断言里还写着旧品牌字符串（`read-frog-*` / `readfrog-*` / `readfrog.app`）           | 改名；能从常量推导的一律改成推导（`APP_NAME`、`env.WXT_WEBSITE_URL`、`PRODUCTION_EXTENSION_ENV_DEFAULTS`），以后不会再漂 |
| 测的是**已移除的功能**（托管内置 AI、Ultra 付费档、Featurebase 反馈、PostHog 遥测） | 删除对应用例；遥测那批换成一条**反向**用例：即使配置齐全且开关打开也绝不上报                                             |
| 夹具给自定义动作挂了纯翻译引擎                                                      | 改挂大模型——自定义动作靠 systemPrompt 抽取，纯翻译引擎跑不了，schema 拒绝它是对的                                        |
| 断言的是被**有意改掉**的交互                                                        | 按新行为重写（如存生词改为静默建库，不再先弹「创建还是连接」）                                                           |

删用例的原则：只删**功能已经不存在**的；类型或字符串漂移一律修，不删覆盖率。

### 本轮又挖出的运行时 bug

1. **`DEFAULT_CONFIG` 通不过 `configSchema`** —— 词典默认挂免密钥的
   Microsoft Translate，但校验只认大模型。后果是 `initializeConfig` 每次启动都
   判定配置无效、**用默认值整个重建，把用户设置静默清空**。老用户没踩到是因为
   词典指向了自己配的大模型；**全新安装（朋友那边）每次重启浏览器都会被重置**。
   已把校验改成与 `resolveDictionaryProviderRef` 同一套规则
2. **点「自定义」复制内置词典没反应** —— 复制出的自定义动作继承了词典的纯翻译
   供应商，写配置时 schema 校验失败抛错，按钮看起来毫无动静。已在复制时兜底换成
   可用的大模型
3. **移除托管 AI 却没有配套迁移** —— 配置版本早于 v088 的用户升级时，
   `v087-to-v088` 会写入 `yangzihao-dic-free-ai`，而本项目里没有这个供应商，
   校验失败 → 配置被重建。已补 `v100-to-v101`：把残留的托管 id 换成这份配置
   自己真实拥有的供应商（翻译类给免密钥引擎，大模型类给已启用的大模型）

### 已知剩余项

- `review/index.tsx` 有一条 `exhaustive-deps` 警告，是**刻意**的：
  只依赖 `nextCard?.id` / `?.front` 而非整个对象，否则每次重新取数都会重复预取语音
