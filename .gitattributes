# 统一行尾为 LF：仓库在 macOS/Linux 上开发，Windows 检出时 autocrlf 会转成
# CRLF，导致 oxfmt 把几乎每个文件都判为格式错误（pre-push 钩子会因此失败）。
* text=auto eol=lf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
*.zip binary
