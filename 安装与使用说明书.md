# 大傻豪学习翻译词典 · 安装与使用说明书

一个划词翻译 + 生词本 + 背单词的浏览器扩展。**完全免费，数据全在你自己电脑上**，
不需要注册账号，不需要联网服务器。

> 本产品完全免费，由 Yang Zihao 开发。

---

## 一、它能干什么

| 功能         | 说明                                   | 要不要 API Key |
| ------------ | -------------------------------------- | -------------- |
| 网页翻译     | 整页双语对照                           | ❌ 不要        |
| 划词翻译     | 选中文字立刻翻译                       | ❌ 不要        |
| 视频字幕翻译 | YouTube 等平台边看边译                 | ❌ 不要        |
| 输入翻译     | 打中文自动转英文                       | ❌ 不要        |
| 划词查词典   | 音标、词性、释义、例句，一键存进生词本 | ⭕️ 建议配      |
| 生词本       | 存在本机，**没有数量上限**             | ❌ 不要        |
| 闪卡复习     | 按遗忘曲线安排复习，三种题型轮换       | ❌ 不要        |
| 造句练习     | 内置 60 句常用表达，看中文拼英文       | ❌ 不要        |
| 学习统计     | 每天学了多少、连续多少天               | ❌ 不要        |
| 语音朗读     | 微软 Edge 语音，音质接近真人           | ❌ 不要        |

**不配 API Key 也能用**：翻译走免费的微软/谷歌翻译，朗读走免费的 Edge 语音，
生词本、闪卡、造句练习全都是本地功能。

只有一个地方值得配 Key：**划词词典**。它要生成音标、词性、释义、例句这些结构化
内容，得靠 AI。不配的话词典会自动降级成"快速模式"——只给翻译，不给词性和例句。

---

## 二、安装步骤

### 第 1 步：下载并解压

拿到 `yangzihao-dic-1.0.0-chrome.zip`，**解压到一个固定的文件夹**。

> ⚠️ 解压后的文件夹不能删、不能改名、不能移动位置。
> 浏览器是直接从这个文件夹读取扩展的，挪走了扩展就失效了。
> 建议放在 `C:\Users\你的用户名\Documents\大傻豪词典\` 这种不会误删的地方。

### 第 2 步：打开扩展管理页

在浏览器地址栏输入并回车：

```
chrome://extensions
```

Edge 浏览器则输入 `edge://extensions`。

### 第 3 步：打开「开发者模式」

页面**右上角**有一个「开发者模式」开关，把它打开。

### 第 4 步：加载扩展

点左上角出现的「**加载已解压的扩展程序**」按钮，选中第 1 步解压出来的文件夹
（要选**包含 `manifest.json` 的那一层**），确定。

看到扩展卡片出现，就装好了。

### 第 5 步：固定到工具栏（可选）

点浏览器右上角的拼图图标 🧩，找到本扩展，点旁边的图钉，
图标就会常驻在工具栏上，用起来更方便。

---

## 三、（可选）配置 AI Key，解锁完整词典

不配也能用，配了词典会更强。以阿里百炼为例：

1. 去 [百炼控制台](https://bailian.console.aliyun.com/) 注册，创建一个 API Key
2. 打开扩展设置页（点扩展图标 → 设置，或右键扩展图标 → 选项）
3. 左侧点「**API 提供商**」→「**添加提供商**」
4. 选「**自定义 Chat Completions API**」，填：
   - **Base URL**：`https://dashscope.aliyuncs.com/compatible-mode/v1`
   - **API Key**：你自己创建的那个
   - **模型**：`qwen-flash`（快）或 `qwen-plus`（准）
5. 往下滚到「**功能提供商**」，把「**词典**」那一行切换到你刚配的这个供应商

> 🔒 **Key 只存在你自己的浏览器里**，不会上传到任何地方——本扩展没有服务器。

### 想更快？

在「功能提供商」里，把**网页翻译 / 划词翻译 / 输入翻译 / 视频字幕**
留在 **Microsoft Translator** 或 **Google Translate**（免费、秒回），
只让**词典**用 AI。这样又快又省。

---

## 四、怎么用

### 划词查词 → 存进生词本

1. 在任意网页选中一个英文单词
2. 弹出的小工具栏里点「**词典**」
3. 看完点「**保存到笔记库**」，这个词连同**它所在的原句**一起存进生词本

> 存的是你**真实读到过**的句子，不是教科书例句——这是后面复习的关键。

### 闪卡复习（背单词）

设置页左侧 →「**闪卡复习**」。按遗忘曲线自动安排，三种题型轮换：

- **听音拼写** — 只放英文语音，把词打出来（练"听得出"）
- **中译英** — 只给中文，写出英文单词（练"想得起"）
- **语境填空** — 给例句中文，把句子里缺的词补上（练"会用"）

答完按数字键 `1`~`4` 打分：`1` 忘了 / `2` 有点难 / `3` 记得 / `4` 太简单。
分数决定下次什么时候再考你。

### 造句练习（练表达）

设置页左侧 →「**造句练习**」。给你中文，**整句英文从零拼出来**，一个提示词都没有，
只有空格的长短暗示单词有几个字母。

内置 **60 句日常常用表达**，装上就能练，不需要任何配置。都是"中文一看就懂、
真要说英文却卡壳"的那种，比如：

- 这不是我能决定的 → `That's not really my call to make.`
- 我现在不太想聊这个 → `I'd rather not get into that right now.`

**答完会展开讲解**，告诉你这句为什么这么说——句式结构、语法要点、外国人的习惯用法。
比如 `I must have missed that part.` 会告诉你 `must have + 过去分词` 是"对过去的
肯定推测"，不是"必须"。

也可以切到「我的生词本」，用你自己存的句子练。

#### 键盘操作（全程不用鼠标）

| 按键              | 作用                     |
| ----------------- | ------------------------ |
| `Enter`           | 提交 / 下一句            |
| `Tab` / `→`       | 下一个空                 |
| `Shift+Tab` / `←` | 上一个空                 |
| `Ctrl` + `'`      | 听整句                   |
| `Ctrl` + `;`      | 显示答案                 |
| `Alt` + `M`       | 标记为已掌握（下轮沉底） |
| `Alt` + `N`       | 标记为生词（下轮优先）   |

空可以**乱序填**——先填后面的、前面留着都行。提交后**只标错的那个空**，
答对的锁定，答错的保留你写的内容让你自己改，**不会直接把答案抖出来**。

---

## 五、常见问题

**Q：需要付费吗？**
完全免费，没有会员、没有内购。唯一可能花钱的是你自己申请的 AI Key
（用不用随你，不用也能正常使用）。

**Q：我的数据会上传吗？**
不会。生词本、复习记录、学习统计**全部存在你自己浏览器里**，本扩展没有服务器。
只有你主动配了 AI Key 时，划词查询的那个词才会发给你自己选的 AI 服务商。

**Q：换电脑了怎么办？**
设置页 →「笔记库」→「**导出 JSON**」备份，新电脑上装好后再导入。

**Q：浏览器提示"请停用开发者模式扩展程序"？**
点「取消」即可，不影响使用。因为这个扩展是手动加载的，不是从应用商店装的。

**Q：更新版本怎么操作？**
下载新的 zip，解压覆盖掉原文件夹，然后到 `chrome://extensions` 点扩展卡片上的
**刷新按钮 ⟳**。你的生词本和复习进度不会丢。

**Q：支持哪些浏览器？**
Chrome、Edge，以及其他基于 Chromium 的浏览器。

---

## 六、开源信息

本项目基于开源项目 [Read Frog](https://github.com/mengxi-ream/read-frog) 二次开发，
遵循 **GPL-3.0** 协议开源。主要改动包括：生词本完全本地化（去掉 20 条上限与账号
体系）、移除全部付费与云端功能、去除数据统计上报、新增闪卡复习与造句练习。

详细的修改说明见项目内的 `NOTICE` 文件。
