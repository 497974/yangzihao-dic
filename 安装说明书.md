# 大傻豪词典 · 安装说明书

浏览器翻译 + 划词词典 + 视频字幕 + 生词本。

**全程本地运行**，除了你自己填的 AI 服务，不向任何服务器发数据。

---

# 第一步：安装插件

## 1. 解压

把收到的压缩包解压出来，会得到一个 **`chrome-mv3`** 文件夹。

> ⚠️ **解压后不要删除、不要移动这个文件夹**。
> 浏览器是直接从这个位置读取插件的，删了插件就没了。
> 建议放在一个固定地方，比如 `D:\插件\chrome-mv3`。

## 2. 打开扩展管理页

在浏览器地址栏（最上面输网址那一条）输入下面这行，按回车：

```
chrome://extensions/
```

> Edge 浏览器输 `edge://extensions/`

## 3. 打开开发者模式

页面**右上角**有个叫「**开发者模式**」的开关，点一下打开（变成蓝色）。

## 4. 加载插件

打开开发者模式后，页面**左上角**会出现几个新按钮。

点最左边的「**加载已解压的扩展程序**」。

弹出文件选择窗口 → 找到刚才解压出来的 **`chrome-mv3`** 文件夹 → 选中它 → 点「选择文件夹」。

## 5. 完成

插件列表里出现「**大傻豪词典**」就装好了。

> 想让图标常驻工具栏：点地址栏右边的拼图图标 🧩，
> 找到大傻豪词典，点它右边的图钉📌。

---

# 第二步：配置 AI（必做）

插件本身不含 AI，需要你自己提供一个 AI 服务的密钥。**这是唯一需要联网的部分。**

## 推荐：阿里云百炼（新用户有免费额度）

### 1. 注册并创建密钥

1. 打开 <https://bailian.console.aliyun.com/>
2. 用支付宝或手机号登录（阿里云账号）
3. 左侧菜单找到「**API Key**」
4. 点「**创建 API Key**」
5. **立刻复制保存**这串密钥

> ⚠️ 密钥只在创建时显示一次，关掉就再也看不到了。
> 没存住的话只能重新创建一个。

### 2. 填进插件

1. 点浏览器工具栏上的大傻豪词典图标
2. 点齿轮 ⚙️ 进入设置
3. 左侧选「**API 服务商**」
4. 点「**添加**」，类型选「**OpenAI 兼容**」
5. 按下表填：

| 字段     | 填什么                                              |
| -------- | --------------------------------------------------- |
| 名称     | 随便起，比如「百炼」                                |
| Base URL | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| 模型     | `qwen3.8-flash`                                     |
| API Key  | 粘贴你刚复制的那串                                  |

6. 点「**测试连接**」——显示成功就配好了

### 3. 指定给各个功能

设置页里把「网页翻译」「视频字幕」「划词词典」的服务商都选成你刚建的那个。

---

## 其他 AI 服务

设置里还内置了 OpenAI、DeepSeek、Claude、硅基流动、OpenRouter 等，
填对应的 Key 即可。也支持任意 OpenAI 兼容的接口。

---

# 第三步：开始用

## 翻译整个网页

点插件图标 → 点「翻译此页」。原文和译文会并排显示。

## 划词查单词

在网页上**选中一个单词或句子** → 冒出小工具条 → 点「**词典**」。

会给出：词条、音标、词性、释义、例句、例句翻译、难度等级。

## 存进生词本

查完词后点「**保存到笔记库**」。

**生词本存在你自己的浏览器里，没有数量上限。**
在设置 → 笔记库里可以查看、搜索、导出 CSV。

## YouTube 双语字幕

打开一个**有字幕**的 YouTube 视频，播放器上会出现插件按钮，点开启用。

> 想要更好的翻译质量，去设置里打开「**AI 智能上下文**」——
> 它会先读完整片字幕生成摘要，再逐句翻译，比逐句盲翻准很多。
> 代价是多消耗一些 token。

---

# 常见问题

**Q：翻译没反应 / 报错**
A：多半是 API Key 没配好。去设置 → API 服务商，点「测试连接」看具体报错。

**Q：提示"认证失败"**
A：两种可能——密钥填错了，或者**免费额度用完了**。
百炼额度耗尽时返回的错误码和认证失败是同一类，插件区分不了。
去百炼控制台看看额度还剩多少。

**Q：视频没有字幕按钮**
A：这个插件只翻译**已有的字幕**，不会凭空生成。
没字幕的视频翻不了。确认视频本身有 CC 字幕。

**Q：换电脑了，生词本怎么办？**
A：数据存在浏览器本地，不会自动同步。
换设备前去设置里**导出 CSV** 备份。

**Q：浏览器更新后插件消失了**
A：开发者模式加载的插件偶尔会掉。重复第一步重新加载即可，数据不会丢。

**Q：会不会偷偷传我的数据？**
A：不会。除了你自己配置的 AI 服务（翻译请求必须发给它），
插件不向任何其他服务器发请求。生词本完全在本地。

---

# 隐私说明

- **生词本**：存在浏览器本地存储里，不上传
- **翻译内容**：只发给你自己配置的 AI 服务商
- **无账号体系**：不需要注册、不需要登录
- **无遥测**：不收集使用数据

---

_大傻豪词典 · 杨子豪_
_基于 GPL-3.0 开源，详见 LICENSE 与 NOTICE_
